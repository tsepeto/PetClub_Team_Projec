jwt:
  secret: '[a-zA-Z0-9._]^+$UltimateSecuritzPasswordForMeEpeidiEimaiAp1steut0$'

server:
  port: 8081
  error:
    whitelabel:
      enabled: false


spring:
  datasource:
    url: jdbc:mysql://ra1.anystream.eu:1790/production_pet_club
    password: 
    username: 
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  banner:
    location:
      classpath:static/images/default/banner.txt

