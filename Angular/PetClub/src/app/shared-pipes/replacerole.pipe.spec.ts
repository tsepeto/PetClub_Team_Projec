import { ReplacerolePipe } from './replacerole.pipe';

describe('ReplacerolePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplacerolePipe();
    expect(pipe).toBeTruthy();
  });
});
